---
apiVersion: apps/v1
kind: Deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ipv4-linkstate-edge
  template:
    metadata:
      labels:
        app: ipv4-linkstate-edge
    spec:
      containers:
        - args:
            - --v
            - "5"
            - --message-server
            - "broker.jalapeno:9092"
            - --database-server
            - "http://arangodb:8529"
            - --database-name
            - "jalapeno"
          image: docker.io/iejalapeno/ipv4-linkstate-edge:latest
          imagePullPolicy: Always
          name: ipv4-linkstate-edge
          volumeMounts:
            - name: credentials
              mountPath: /credentials
      volumes:
        - name: credentials
          secret:
            secretName: jalapeno
metadata:
  name: ipv4-linkstate-edge
  namespace: jalapeno
